/**
 * Shared type definitions
 */

import type { HonoContext } from '../index'

/**
 * Context type for routes
 * Re-export the HonoContext with a shorter name for convenience
 */
export type Context = HonoContext
